modelEncoding: UTF-8
modelVersion: 4.0.0
groupId: com.github.stcarolas.enki
artifactId: enki
version: 1.0.0
packaging: pom
name: Enki
properties: {maven.compiler.target: 14, java.version: 14, maven.compiler.source: 14,
  revision: 1.2.3}
modules: [core, server, providers/github, providers/gitea, providers/gitlab, providers/one-repo,
  providers/bitbucket, handlers/discord-commit-hook, handlers/gitea-mirror, handlers/github-archive-repo,
  handlers/gocd-handlers, handlers/jacoco, handlers/logging-handlers, handlers/maven-dependency-handlers,
  tools/generator, tools/discord-bot]
