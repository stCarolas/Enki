modelEncoding: UTF-8
modelVersion: 4.0.0
groupId: com.github.stcarolas.enki
artifactId: enki
version: 0.1.27
packaging: pom
name: Enki
modules: [core, server, tools/cli-runner, tools/discord-bot, tools/generator, providers/github,
  providers/gitea, providers/gitlab, providers/one-repo, providers/bitbucket, handlers/discord-commit-hook,
  handlers/gitea-mirror, handlers/github-archive-repo, handlers/gocd-handlers, handlers/jacoco,
  handlers/logging-handlers, handlers/maven-dependency-handlers]
