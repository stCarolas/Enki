gitea {
    url="http://git.service.consul/api/v1"
    username=${GITEA_USER}
    password=${GITEA_PASSWORD}
    organization=Mango
}
discord {
    token=${DISCORD_TOKEN}
    server=${DISCORD_SERVER}
    categoryId=${DISCORD_CATEGORY}
}
